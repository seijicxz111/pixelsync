{
  "name": "@pixelsync/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev --port 3000",
    "build": "next build",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@pixelsync/shared": "workspace:*",
    "@pixelsync/ui": "workspace:*"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4"
  }
}
